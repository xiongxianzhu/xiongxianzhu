# 仓库协作约定

本仓库用于展示 GitHub 个人资料，不是应用项目。使用简洁中文，技术名保留英文。本文件只补充仓库约定；遵循系统、开发者及用户指令，不额外扩大授权。

## 内容与设计

- `README.md` 是主页入口，使用 GitHub 支持的 Markdown 和 HTML。
- 项目卡片采用扁平设计、2px 圆角，适配深浅主题；使用石墨灰、雾蓝及少量暖金、淡紫，不使用绿色。
- 项目名单、顺序和视觉分类位于 `scripts/generate-project-cards.py`。简介、主语言、Star 和 Fork 来自 GitHub API，不手填或猜测。
- `assets/projects/` 是生成结果。修改卡片时更新生成脚本并重新生成，避免只改 SVG 后被自动更新覆盖。
- `.github/workflows/update-project-cards.yml` 负责定时同步。只在调整自动更新行为时修改它。

## 工作范围与边界

- 按当前任务读取相关文件，不要求每次通读仓库、全部技能或参考资料。
- 明确的本地编辑、生成和验证可直接完成；只有缺失信息会实质改变结果，或操作超出已有授权时才询问。已授权事项不重复确认。
- 简单文案、链接和样式调整直接实施，不额外要求设计文档、分阶段审批或多 Agent 流程。
- 保留用户已有修改，只处理当前请求。发现无关问题时简要说明，不顺带重构。
- 提交、推送或发布按用户明确授权执行；本文件不要求每次修改后自动提交。已配置的定时工作流按其自身权限运行。
- 不为等待定时任务而空等。需要确认运行结果时，在已有授权内主动验证；环境阻塞时完成不受影响的部分，说明阻塞原因。

## 验证与完成

- 文案或链接调整：检查受影响内容、锚点、相对路径及 `git diff --check`，无需重新获取项目数据。
- 卡片布局调整：运行 `python scripts/generate-project-cards.py`，检查生成结果，并抽查深浅主题、长标题与长简介的显示。
- 数据获取逻辑调整：针对成功响应、缺失字段和请求失败验证；失败时保留旧卡片。可用 `--data-file <JSON文件>` 离线验证，并如实说明未验证的网络部分。
- 工作流调整：检查 YAML、触发条件、权限及调用命令；没有实际运行时，不声称线上验证通过。
- 请求的改动完成、相关检查通过且没有未解决的相关问题，即可交付。只有新改动、失败或具体疑点才需要扩大或重复验证。
- 最终简述改动、验证结果及未完成事项；涉及自动同步时，区分本地生成成功与远端工作流已生效。

## 维护原则

只保留有实际作用的仓库约定；发现重复或冲突时优先合并、缩小适用范围，不继续叠加流程。

参考：[OpenAI：Rethinking skills and prompts for GPT-6 Astra](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)。该链接用于说明设计依据，不要求每次任务重新阅读。
